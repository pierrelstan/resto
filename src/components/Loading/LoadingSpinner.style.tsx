import { TailSpin } from 'react-loader-spinner';
import styled from 'styled-components';

export const Loading = styled(TailSpin)``;
export const Container = styled.div`
  display: flex;
  justify-content: center;
`;
